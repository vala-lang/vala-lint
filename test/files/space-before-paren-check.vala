// vala-lint=skip-file

void test () { }
void test() { }

int main (string[] args) {
    var test = 2 * (3 + 1);
    bool a = !(true && false);
    int b = 2*(3+2);
    var actions &= ~(Gdk.DragAction.COPY | Gdk.DragAction.LINK);

    return 0;
}
